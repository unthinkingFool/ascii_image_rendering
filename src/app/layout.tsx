import type { Metadata } from "next";
import { Geist_Mono } from "next/font/google";
import "./globals.css";

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "ASCII Vision — Real-Time ASCII Art Renderer",
  description:
    "Transform live camera, images, and videos into vibrant ASCII art in real-time. AI-powered subject isolation, customisable character sets, and instant export.",
  keywords: ["ASCII art", "real-time", "camera", "AI segmentation", "web app"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={`${geistMono.variable} h-full antialiased`}>
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
